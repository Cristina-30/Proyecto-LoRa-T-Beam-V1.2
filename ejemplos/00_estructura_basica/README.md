# 00 - Estructura básica de un programa Arduino

Primer sketch del proyecto.  
Sirve para entender las funciones obligatorias `setup()` y `loop()`.

**Objetivo:** Familiarizarse con la estructura básica de Arduino.
